//lib\widgets\BotonIrATareas.dart
/*
import 'package:flutter/material.dart';
import '../screens/tareas_screen.dart'; // Importa la pantalla de tareas

class BotonIrATareas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(Icons.assignment), // Utiliza un icono representativo
      color: Colors.white,
      onPressed: () {
        // Navega a la pantalla de tareas
        Navigator.push(
          context,
          MaterialPageRoute(builder: (context) => TareasScreen()),
        );
      },
    );
  }
}
*/
